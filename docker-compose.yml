x-kanbancw-config: &kanbancw-config
  KANBANCW_DOMAIN: ${KANBANCW_DOMAIN}
  CHATWOOT_DOMAIN: ${CHATWOOT_DOMAIN}
  CHATWOOT_DATABASE_URL: ${CHATWOOT_DATABASE_URL}

services:
  postgres-kanban:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: kanbancw
      POSTGRES_USER: kanbancw
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - kanbancw-postgres:/var/lib/postgresql/data
    networks:
      - network_public
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M

  redis-kanban:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - kanbancw-redis:/data
    networks:
      - network_public
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

  backend:
    image: luantuyter/globalveiculos-backend:1.0
    environment:
      NODE_ENV: production
      PORT: 3000
      LOG_LEVEL: info
      DATABASE_URL: postgresql://kanbancw:${POSTGRES_PASSWORD}@postgres-kanban:5432/kanbancw?schema=public
      UPLOAD_DIR: /app/uploads
      REDIS_HOST: redis-kanban
      REDIS_PORT: 6379
      CORS_ORIGIN: "*"
      JWT_SECRET: ${JWT_SECRET}
      DISABLE_AUTH: ${DISABLE_AUTH:-false}
      DEFAULT_TENANT_ID: ${DEFAULT_TENANT_ID:-}
      <<: *kanbancw-config
    volumes:
      - kanbancw-uploads:/app/uploads
    networks:
      - network_public
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 10s
        order: start-first
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.kanbancw-backend.rule=Host(`${KANBANCW_DOMAIN}`) && (PathPrefix(`/api`) || PathPrefix(`/webhooks`) || PathPrefix(`/health`) || PathPrefix(`/socket.io`) || PathPrefix(`/dashboard-script`) || PathPrefix(`/uploads`))"
        - "traefik.http.routers.kanbancw-backend.entrypoints=websecure"
        - "traefik.http.routers.kanbancw-backend.tls=true"
        - "traefik.http.routers.kanbancw-backend.tls.certresolver=letsencryptresolver"
        - "traefik.http.routers.kanbancw-backend.priority=100"
        - "traefik.http.services.kanbancw-backend.loadbalancer.server.port=3000"
        - "traefik.docker.network=network_public"

  frontend:
    image: luantuyter/globalveiculos-frontend:1.0
    networks:
      - network_public
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 10s
        order: start-first
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 64M
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.kanbancw-frontend.rule=Host(`${KANBANCW_DOMAIN}`)"
        - "traefik.http.routers.kanbancw-frontend.entrypoints=websecure"
        - "traefik.http.routers.kanbancw-frontend.tls=true"
        - "traefik.http.routers.kanbancw-frontend.tls.certresolver=letsencryptresolver"
        - "traefik.http.services.kanbancw-frontend.loadbalancer.server.port=80"
        - "traefik.docker.network=network_public"
        - "traefik.http.routers.kanbancw-frontend.priority=1"

volumes:
  kanbancw-postgres:
    external: true
    name: kanbancw-postgres
  kanbancw-uploads:
    external: true
    name: kanbancw-uploads
  kanbancw-redis:
    external: true
    name: kanbancw-redis

networks:
  network_public:
    external: true
